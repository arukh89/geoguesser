NEXT_PUBLIC_MAPILLARY_TOKEN=MLY|your_access_token_here

# SpacetimeDB connection URI
# CRITICAL: Must differ from Next.js dev server port (default 3000)
# Recommended: Run SpacetimeDB on port 3010
NEXT_PUBLIC_STDB_URI=http://127.0.0.1:3010

# Optionally set base URL when behind proxies or custom domains
# NEXT_PUBLIC_BASE_URL=http://localhost:3000
